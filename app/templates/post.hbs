<div class="container post-container py-4">
  <img class="card-img-top" src={{model.image}} alt="Card image cap">
  <h1 class="mt-5">{{model.title}}</h1>
  <p class="my-4">{{model.body}}</p>

  <h4 class="my-5">Comments</h4>

  <ul>
    {{#each @model.comments as |comment|}}
      <li>
        {{comment.name}}<br>
        {{comment.body}}<br>
      </li>
    {{/each}}
  </ul>
</div>